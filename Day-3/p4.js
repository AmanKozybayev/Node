 for (let i = 0; i < 10; i++) {// declaring array that will contain rows 
    let arr = []; // every roww is an array 
    for (let j = 0; j < 5; j++) { // this is the columns 
        arr.push(j);// pushing numbers to every row 
    }
    console.log(arr);// display array 
 }
 