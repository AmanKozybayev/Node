const obj = {
    type: "small",
    index: 1, 
    size: 2, 
    companies: ["Coca-cola","Google", "Ford"],
};

const companies = {
    data: ["Coca-cola", "Google", "Ford"], //data and length are the keys of the object 
    length: 3, // on the right side of the key are values of the object 
};

const ar = ["A", "B"];
console.log(companies.data);
console.log(ar.length);

console.log(obj.index);
